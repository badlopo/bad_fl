import 'package:flutter/material.dart';

class WebviewFragment extends StatelessWidget {
  const WebviewFragment({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
