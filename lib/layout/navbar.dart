// 实现类 Manager
// - 初始化: 传入一个 int[], (确保其中的各个元素>=0且无重复)
// - swap(x, y) -- 把 x 和 y 的位置互换 (确保x、y在列表中)
// - reorder(int[]) -- 将列表重排为参数的形式(也就是替换成新的列表)
// - sort(HashMap[]) -- 每个元素有一个键 'key', 值为 int. 值在列表中的元素按照列表中的顺序进行排序, 剩余的保留原顺序放到末尾
// - add(int) -- 添加一个元素到列表尾部
// - addAll(int[])  -- 全部添加到列表尾部

// TODO