import 'package:flutter/material.dart';

class BadTree extends StatelessWidget {
  const BadTree({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
