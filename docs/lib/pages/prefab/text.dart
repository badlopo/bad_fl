import 'package:bad_fl/prefab/text.dart';
import 'package:flutter/material.dart';

class BadTextDocPage extends StatelessWidget {
  const BadTextDocPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const BadText('BadText')),
    );
  }
}
